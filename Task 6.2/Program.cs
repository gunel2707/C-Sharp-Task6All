
namespace Task_6._2
{
    internal class Program
    {
        static void Main(string[] args)
        {

            // Istifadeciden bir array alin. Hemin array de tekrarlanan elementleri capa verin. + hansi indexlerde tekrarlanibsa onlarida capa verin


            //bool t = false;

            //int[] ints = new int[8];
            //for (int i = 0; i < ints.Length; i++)
            //{
            //    Console.Write("ints[" + i + "]="); ints[i] = int.Parse(Console.ReadLine());
            //}

            //for (int i = 0; i < ints.Length; i++)
            //{

            //    for (int j = i + 1; j < ints.Length; j++)
            //    {
            //        if (ints[i] == ints[j])
            //        {
            //            t = true; Console.WriteLine("tekrar element:" + ints[i] + ",index" + j + "," + i);

            //        }
            //    }

            //}
            //if (!t) { Console.WriteLine("tekrarlanma yoxdur"); }



//----------------------------------------------------------------------------------------
           
            
            //int[] ints = new int[10];
            //for (int i = 0; i < ints.Length; i++)
            //{
            //    Console.Write("ints[" + i + "]="); ints[i] = int.Parse(Console.ReadLine());
            //}

            //foreach (var item in ints.GroupBy(x => x))
            //    Console.WriteLine(item.First() + " " + item.Count() + " eded movcuddur");
            //Console.ReadLine();
            //Console.ReadKey();

//------------------------------------------------------------------------------------

            // Tekrarlanma ardicil olduqda icra olunur
            //    int c = 1;
            //    int[] ints = new int[4];
            //    for (int i = 0; i < ints.Length; i++)
            //    {
            //        Console.Write("ints[" + i + "]="); ints[i] = int.Parse(Console.ReadLine());
            //    }
            //    for (int i = 1; i < ints.Length; i++) {
            //        if (ints[i] == ints[i - 1]) {
            //            c += 1; Console.WriteLine("tekrar element:"+ints[i]+",index"+(i-1)+","+i);
            //        }

            //    }
            //    Console.WriteLine("tekrarlanma sayi:" + c);




        }
    }
}



    
    

